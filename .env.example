# Backend API URL (optional for local dev - Vite proxy uses /api -> localhost:5000)
# For production (e.g. Vercel), set this to your deployed backend URL including /api
# Example: https://your-backend.onrender.com/api
VITE_API_URL=
